# FEATURE_SPEC.md

> **파일명**: FEATURE_SPEC.md  
> **최종 수정일**: 2026-07-05 (위험군 "5단계" 잔존 표기를 4단계로 정정 — F-01/F-03/F-14/F-17)  
> **문서 해시**: SHA256:1edc22251e82d0bc0315c16513cc47369450fe04ce7d14c7c9b0d0a85efc496f
> **문서 역할**: 기능별 입력, 출력, 처리 규칙, 예외, 상태 정의 문서  
> **문서 우선순위**: 4  
> **연관 문서**: PRD.md, SYSTEM_ARCHITECTURE.md, API_SPEC.md, DATA_SCHEMA.md, RAG_PIPELINE.md  
> **참조 규칙**: 기능을 추가, 제거, 변경하거나 사용자 흐름을 바꿀 때 먼저 이 문서를 수정한다.

---

## 1. 문서 목적

이 문서는 시스템이 제공하는 기능을 **사용자 기능 단위**와 **운영 기능 단위**로 정의한다.  
각 기능은 아래 기준으로 명세한다.

- 기능 목적
- 주 사용자
- 입력
- 출력
- 처리 규칙
- 예외 상황
- 현재 상태(활성 / reserved)

이 문서는 기능 정의 문서이며, 다음 항목은 직접 다루지 않는다.

- 제품 문제 정의와 사용자 가치
- 시스템 계층 구조와 모듈 책임
- API endpoint 상세 형식
- 데이터 필드의 컬럼 단위 구조
- retrieval 파이프라인 세부 전략
- 프롬프트 세부 문구

위 항목은 각각 `PRD.md`, `SYSTEM_ARCHITECTURE.md`, `API_SPEC.md`, `DATA_SCHEMA.md`, `RAG_PIPELINE.md`, `PROMPT_DESIGN.md`에서 담당한다.

---

## 2. 기능 상태 정의

### 활성
현재 MVP 범위 안에서 실제 구현 및 연결 대상인 기능

### reserved
후속 스프린트에서 활성화할 예정인 기능  
현재 단계에서는 미구현 또는 비활성 상태를 유지한다.

---

## 3. 기능 분류

현재 기능은 아래 7개 그룹으로 나눈다.

1. 사용자 입력 및 추천 진입
2. 추천 결과 생성
3. 로드맵 생성
4. 설명 근거 제공
5. 운영자/배치 갱신
6. 실행 지원 (Execution Layer — 공공데이터 API 연동)
7. 관리자 기능 (reserved)

---

## 4. 기능 목록 요약

| 기능 ID | 기능명 | 현재 상태 | 주 사용자 |
|---|---|---:|---|
| F-01 | 위험군 기반 추천 진입 (12문항 설문) | 활성 | 사용자 |
| F-02 | 관심 도메인·직무 선택 (InterestSelection) | 활성 (도메인 완료 / 직무 UI 추가 필요) | 사용자 |
| F-03 | 자격증 후보 결과 조회 | 활성 | 사용자 |
| F-04 | 추천 이유 / 설명 근거 조회 | 활성 | 사용자 |
| F-05 | 위험군 맞춤 로드맵 조회 | 활성 | 사용자 |
| F-06 | CSV canonicalization 실행 | 활성 | 운영자/배치 |
| F-07 | entity / relation / candidate 재생성 | 활성 | 운영자/배치 |
| F-08 | 시험 일정 조회 (Q-Net API) | 활성 | 사용자 |
| F-09 | 접수 일정 / 지원 링크 조회 (Q-Net API) | 활성 | 사용자 |
| F-10 | 관리자 점검 화면 | reserved | 운영자 |
| F-11 | 관련 동영상 추천 (YouTube) | 활성 | 사용자 |
| F-12 | WorkNet 채용정보 조회 (cert_id 기반) | 활성 | 사용자 |
| F-13 | Work24 훈련과정 조회 (cert_id 기반) | 활성 | 사용자 |
| F-14 | 과정평가형 자격 조회 | 활성 | 사용자 |
| F-15 | 서울시 공공 자원 조회 (일자리카페·건강증진센터·공공예약) | 활성 | 사용자 |
| F-16 | 오늘의 한 가지 행동 제안 (위험군 단계 기반) | 활성 | 사용자 |
| F-17 | 취업지원 자원 번들 조회 (위험군 × 도메인/직무 기반) | 활성 | 사용자 |
| F-18 | 고립군 청년 군집 분류 (12문항 → 4개 군집) | 활성 | 사용자 |
| F-19 | 고립군 군집별 맞춤 정책 추천 번들 (서비스 매트릭스 기반) | 활성 | 사용자 |
| F-20 | 복지로 중앙부처복지서비스 조회 | 활성 | 사용자 |
| F-21 | 복지로 지자체복지서비스 조회 | 활성 | 사용자 |
| F-22 | 건강가정지원센터 시설 조회 | 활성 | 사용자 |
| F-23 | 여성·가족·청소년·권익시설 조회 | 활성 | 사용자 |
| F-24 | 워크넷 정부지원일자리 참여자모집 조회 | 활성 | 사용자 |
| F-25 | DIDIM 서비스 허브 — 군집 기반 전체 서비스 활성/비활성 + LLM 설명 | 활성 | 사용자 |
| F-26 | 온통청년 청년정책 조회 (대분류·중분류·지역 필터) | 활성 | 사용자 |
| F-27 | 온통청년 청년공간(센터) 조회 (지역·유형 필터) | 활성 | 사용자 |
| F-28 | 온통청년 청년콘텐츠 조회 (대분류 카테고리별 분류) | 활성 | 사용자 |
| F-29 | 일학습병행 훈련과정 조회 (Work24 313L01) | 활성 | 사용자 |
| F-30 | 국가인적자원개발 컨소시엄 훈련과정 조회 (Work24 312L01) | 활성 | 사용자 |
| F-31 | 구직자취업역량 강화프로그램 조회 (키 발급 완료, 엔드포인트 승인 대기) | 대기 | 사용자 |
| F-32 | 자격증·진로 Q&A 챗봇 (RAG 근거 기반, 자체 검증) | 활성 (범위 한정 — §5 F-32 범위 경계 참고) | 사용자 |
| F-33 | 재방문 이어가기 + 오늘의 행동 스트릭 (localStorage) | 활성 | 사용자 |

---

## 5. 기능 상세

## F-01. 위험군 기반 추천 진입 (설문 방식)

### 목적
12문항 설문을 통해 사용자의 위험군 단계(1~4)를 판정하고 추천 흐름을 시작한다.

### 주 사용자
- 사용자

### 입력
12문항 설문 응답 (각 문항은 카테고리별 점수 옵션으로 구성)

| 카테고리 | 문항 수 | 주요 측정 내용 |
|---|:---:|---|
| 관계망 | 4 | 조언·도움·감정 공유 가능한 사람 유무, 지인 만남 빈도 |
| 활동 | 2 | 외출 빈도, 대화 빈도 |
| 노동·경제 | 2 | 취업 상태, 경제 상황 인식 |
| 정신건강 | 3 | 우울감, 흥미 저하, 자살·자해 사고 (Safety Key) |
| 자기관리 | 1 | 식사·위생·정리 등 일상 관리 수준 |

### 출력
- 판정된 위험군 단계 (1~4)
- 카테고리별 점수 요약
- Safety Override 활성 여부

### 처리 규칙
- 위험군은 1단계 ~ 4단계 구조를 따른다 (`risk_0005`는 백엔드에 비활성 보존, 프론트엔드 미사용).
- 단계 판정: `총점 / 최대점수` 비율이 25% 미만이면 1단계, 50% 미만이면 2단계로 확정한다. 50% 이상이면 2차 정밀 3문항(`SCRIPT.md` §4)의 응답 조합(`P_Q1 ≥ ④ AND (P_Q2 = ① OR P_Q3 ≥ ③)`)으로 3단계·4단계를 구분한다.
- **Safety Override**: 정신건강 항목 중 `B12_9`(자살·자해 사고)에서 score ≥ 2 ("일주일 이상") 이상 응답 시 Safety Flag를 활성화한다. Safety Flag 활성 시 최소 4단계로 조정한다.
- Safety Flag 활성 시 위기 상담 기관 안내 배너(1393 자살예방상담전화)를 함께 표시한다.
- 판정된 위험군 단계는 이후 F-02(도메인·직무 선택) 진입 시 URL 파라미터로 전달한다.

### 예외 상황
- 미응답 문항이 있는 상태에서 결과 보기 시도 → 미응답 문항으로 이동
- 허용 범위를 벗어난 단계값
- 전체 재진단 요청 (다시 진단 버튼)

### 현재 상태
활성

---

## F-02. 관심 도메인·직무 선택 (InterestSelection)

### 목적
F-01에서 판정된 위험군 단계를 바탕으로, 사용자가 관심 도메인과 직무를 선택하여 추천 맥락을 완성한다.

### 주 사용자
- 사용자

### 입력
- F-01에서 전달된 위험군 단계 (URL 파라미터 `stage`)
- 관심 도메인 선택 (필수 — domain taxonomy 세부 라벨 중 1개)
- 관심 직무 선택 (선택 — job taxonomy 세부 직무 중 1개 이상)

### 출력
- 선택된 도메인 ID (`domain_XXXX`)
- 선택된 직무 ID 목록 (`job_XXXX`) (선택 입력)
- F-05(로드맵 조회)로 전달할 맥락: `stage` + `domain` + `domainName` (+ `job`이 있으면 추가)

### 처리 규칙
- `related_domains`는 허용된 domain taxonomy 세부 라벨만 사용한다. 자유 텍스트 도메인 생성 금지.
- `related_jobs`는 허용된 희망 직무 taxonomy 세부 직무만 사용한다. 자유 텍스트 직무 생성 금지.
- 도메인은 단일 선택 필수, 직무는 선택적으로 추가한다.
- 도메인 선택 없이 다음 단계로 이동할 수 없다.
- 도메인만 선택하고 직무를 선택하지 않아도 로드맵 진입 가능하다 (위험군 × 도메인 기반 추천).
- 선택된 값은 URL 파라미터로 Roadmap으로 전달한다 (store 미사용, URL 기반 상태 전달).

### 예외 상황
- 도메인 미선택 상태에서 다음 이동 시도 → 버튼 비활성화
- 직무/도메인 값이 taxonomy 밖 값인 경우 → 오류로 간주, 선택 불가 처리
- 위험군 단계 파라미터 누락 시 → 단계 없이 도메인·직무 기준만으로 추천 진입

### 현재 상태
활성 (도메인 선택 구현됨 / 직무 선택 UI는 추가 구현 필요)

---

## F-03. 자격증 후보 결과 조회

### 목적
정규화된 추천 맥락을 바탕으로 자격증 후보를 반환한다.

### 주 사용자
- 사용자

### 입력
- 위험군 단계
- 정규화된 관심 직무
- 정규화된 관심 도메인
- 추천 요청 맥락

### 출력
추천 후보 리스트. 각 후보는 최소 아래 정보를 포함한다.

- 자격증 식별자
- 자격증명
- 관련 직무
- 관련 도메인
- 연결 가능한 로드맵 단계
- 추천 요약

### 처리 규칙
- 후보 검색은 canonical recommendation 데이터(JSONL·DB 등 **실행 단계에서 확정**)를 사용한다. **현재는 준비만** — 스키마·예시 파일(`candidates.jsonl.example`)·문서 계약을 갖춘다.
- 추천 결과는 taxonomy 밖 라벨을 포함하지 않아야 한다.
- roadmap_stage(구조적 단계, §F-05)는 기존과 동일하게 `cert_grade_tier`/합격률 기반 난이도 서열로만 배정한다: **위험군 단계가 높을수록(4단계) 낮은 등급(기능사·산업기사)을 우선 배정하고, 기사·기술사는 후순위 stage로 배정한다.** 위험군 단계가 낮을수록(1~2단계) 기사 이상도 동등 후보로 포함한다. `cert_grade_tier`가 null인 자격증은 위험군 단계와 무관하게 이 서열 기준 밖 후보로 처리한다(합격률 기반 대체 규칙 적용).
- **같은 roadmap_stage 안에서 어떤 자격증을 먼저·더 많이 보여줄지는 §3.1 "적합도 지표(Fit Score)"로 정렬한다.** stage 배정 자체는 바뀌지 않는다 — stage 내부 우선순위만 결정한다.

### 3.1 적합도 지표 (Fit Score)

**목적**: "난이도만 쉬운 순"이 아니라 "이 사용자에게 잘 맞는 순"으로 같은 roadmap_stage 내부 자격증 순위를 매긴다. 취업 실용성·인기·위험군 단계별 감당 가능한 난이도를 함께 반영한다.

**입력**: 후보의 `related_domains`/`related_jobs`, 사용자의 `domain_ids`/`job_ids`, `risk_stage_id`, 후보의 `avg_pass_rate_3yr`, 전공별 취득 현황 누적 취득자 수(`accumAcquCnt`), `exam_frequency`.

**구성 요소** (0.0~1.0 스케일, 가중합):

| 요소 | 가중치 | 정의 |
|---|---|---|
| 관심 일치도 (interest_match) | 0.45 | 후보의 `related_domains`/`related_jobs`가 사용자가 고른 `domain_ids`/`job_ids`와 얼마나 겹치는지. 도메인+직무 둘 다 지정되고 둘 다 일치 → 1.0, 도메인만 일치 → 0.7, 직무만 일치 → 0.55, 하나만 지정했는데 불일치(대체 경로로 유입) → 0.3, 아무 관심 정보 없이 위험군 단독 조회 → 0.5(중립) |
| 취업 실용성 (employability) | 0.35 | `0.5×인기도`(누적 취득자 수 log 정규화) + `0.3×직무 폭`(`related_jobs` 개수 정규화, 상한 8개) + `0.2×응시 접근성`(연간 시험 횟수 정규화) |
| 난이도 적합도 (difficulty_fit) | 0.20 | 위험군 단계별 "부담 없는" 목표 합격률과 후보 합격률의 근접도. 목표 합격률은 위험군이 심할수록(4단계) 높게(=쉬운 자격증 우대), 위험군이 가벼울수록(1단계) 낮게(=난이도보다 관심·실용성 비중을 더 둠) 잡는다: `risk_0001=40%, risk_0002=48%, risk_0003=58%, risk_0004=68%`. 합격률 미상이면 중립(0.5). |

`fit_score = 0.45×interest_match + 0.35×employability + 0.20×difficulty_fit` (0.0~1.0, 응답에는 0~100 정수로 노출).

**적용 규칙**:
- roadmap_stage 내부 정렬 키: `(-fit_score, level_score)` — fit_score 내림차순, 동점일 때만 기존 난이도 서열로 tie-break.
- 전체 순서형 리스트(`roadmap_sequence`)는 `(stage_order, -fit_score, level_score)` 순으로 정렬 — stage 진행 순서는 유지하되 같은 stage 안에서는 fit_score가 우선한다.
- `top_n_per_stage` 절단(기본 5건)은 fit_score 정렬 이후에 적용한다 — 즉 같은 stage 후보가 많을 때 실제로 화면에 노출되는 건 fit_score 상위 건이다.
- 가중치(0.45/0.35/0.20)와 목표 합격률 표는 **v1 기본값**이며, 골든셋 검증 후 조정 가능한 정책 값으로 취급한다(하드코딩된 최종값 아님).

### 예외 상황
- 추천 후보 데이터 미존재
- 추천 결과 0건
- canonical store 연결 실패
- 추천 결과에 taxonomy 밖 값 포함
- domain_ids/job_ids 미지정 시 interest_match는 중립값(0.5)으로 처리되어 취업 실용성·난이도 적합도만으로 정렬됨

### 현재 상태
활성

---

## F-04. 추천 이유 / 설명 근거 조회

### 목적
추천 결과에 대해 공식 PDF / HTML 문서 기반 설명 근거를 제공한다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자
- 선택 입력: 위험군 단계
- 선택 입력: 관련 도메인
- 선택 입력: 관련 직무
- 추천 맥락

### 출력
- 근거 텍스트 snippet
- 관련 문서 식별자
- 선택적 provenance 정보

### 처리 규칙
- 근거 검색은 PDF / HTML 기반 retrieval 계층을 사용한다.
- 구조적 추천 결과와 설명 근거는 분리된 계층에서 생성된다.
- 설명 근거가 없더라도 추천 결과 자체는 반환 가능해야 한다.
- 근거는 추천 이유를 보완하는 용도로 제공한다.

### 예외 상황
- retrieval 결과 0건
- 문서 파싱/인덱싱 누락
- source freshness 문제
- exact match 실패

### 현재 상태
활성

---

## F-05. 위험군 맞춤 로드맵 조회

### 목적
추천 결과 또는 위험군/도메인 기준으로 단계형 로드맵을 생성한다.

### 주 사용자
- 사용자

### 입력
- 위험군 단계
- 선택 입력: 자격증 식별자
- 선택 입력: 관련 도메인
- 선택 입력: 관련 직무

### 출력
- 로드맵 단계 리스트
- 단계별 설명
- 단계별 연결 자격증 또는 도메인 힌트

### 처리 규칙
- 로드맵은 위험군 단계 기준과 canonical relation 기준을 결합해 생성한다.
- **로드맵은 flat list가 아니라 `cert_prerequisite` 관계(§6.8)를 방향 그래프(DAG)로 순회하여 생성한다.** 사용자 현재 위치(보유 자격증 또는 위험군 단계)에서 실제 이동 가능한 경로만 단계 후보로 포함한다.
- DAG 순회 시 사이클이 존재하는 경우 해당 edge는 무효로 처리하고 순회를 중단한다.
- 현재 단계에서는 일정/링크 없이도 로드맵 결과가 성립해야 한다.
- 로드맵 단계명은 roadmap master 기준을 따른다.
- 단계별 연결 자격증 순서는 F-03 §3.1 "적합도 지표(Fit Score)" 기준으로 정렬한다(난이도 서열은 stage 배정에만 사용, stage 내부 순위는 fit_score 우선).

### 예외 상황
- roadmap stage 데이터 누락
- 위험군과 로드맵 연결 관계 없음
- 도메인/직무 기준 불충분

### 현재 상태
활성

---

## F-06. CSV canonicalization 실행

### 목적
원본 CSV를 정규화된 entity / relation / candidate 구조의 기반 데이터로 변환한다.

### 주 사용자
- 운영자
- 배치 작업

### 입력
- CSV 파일 세트
- dataset type registry
- schema mapping registry
- taxonomy 기준 파일

### 출력
- canonical entity 결과
- canonical relation 결과
- validation report

### 처리 규칙
- CSV는 Parse IR 대상이 아니다.
- dataset type 판별 후 schema mapping을 수행한다.
- 값 정규화, alias 정리, taxonomy 제한 검증을 수행한다.

### 예외 상황
- dataset type 미식별
- schema mapping 실패
- invalid field 값
- taxonomy 밖 값 생성
- source row 누락

### 현재 상태
활성

---

## F-07. entity / relation / candidate 재생성

### 목적
정규화된 데이터를 바탕으로 recommendation candidate row를 재생성한다.

### 주 사용자
- 운영자
- 배치 작업

### 입력
- canonical entity 결과
- canonical relation 결과
- risk stage master
- roadmap stage master

### 출력
- recommendation candidate row
- candidate validation report

### 처리 규칙
- candidate row는 recommendation core의 기본 검색 단위다.
- `related_domains`는 domain taxonomy 세부 라벨만 허용한다.
- `related_jobs`는 희망 직무 taxonomy 세부 직무만 허용한다.
- 일정 API 관련 필드는 현재 reserved/null 허용이다.

### 예외 상황
- entity / relation 누락
- candidate 생성 0건
- 중복 candidate 생성
- required field 누락

### 현재 상태
활성

---

## F-08. 시험 일정 조회 (Q-Net API)

### 목적
추천된 자격증과 연결된 시험 일정을 Q-Net 공공 API로 실시간 조회한다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자 (`cert_id`)

### 출력
- 시험 일정 목록 (회차, 필기시험 기간, 실기시험 기간, D-Day 포함)

### 처리 규칙
- `cert_id` → `cert_master.csv`에서 `cert_name` 조회 → Q-Net `getExamScheduleList` API 호출
- D-Day 계산: `YYYYMMDD` 형식 날짜 → 오늘로부터 남은 일수
- API 키: `hrdkorea_api_key_in` (URL-encoded)
- 결과 없음(0건)은 오류가 아니다. 빈 배열로 반환한다.

### 예외 상황
- API 키 미설정 → `API_KEY_MISSING` 오류
- API 응답 timeout → `EXTERNAL_API_TIMEOUT` 오류
- cert_id 미존재 → `CERT_NOT_FOUND` 오류
- 일정 데이터 없음 → 빈 배열 반환 (오류 아님)

### 현재 상태
활성

---

## F-09. 접수 일정 / 지원 링크 조회 (Q-Net API)

### 목적
추천된 자격증과 연결된 접수 일정 및 Q-Net 공식 지원 링크를 보여준다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자 (`cert_id`)

### 출력
- 접수 가능 일정 목록 (현재 접수 중인 회차 우선)
- Q-Net 지원 URL (`https://www.q-net.or.kr/man001.do?imenseNo={imenseNo}&gSite=Q`)

### 처리 규칙
- Q-Net `getExamScheduleList` API를 호출하여 `doc_reg_start_dt` ≤ 오늘 ≤ `doc_reg_end_dt`인 회차를 필터링한다.
- 지원 링크는 `imenseNo`를 이용해 Q-Net 표준 URL로 구성한다 (외부 날짜/링크를 지어내지 않는다).
- API 키: `hrdkorea_api_key_in`

### 예외 상황
- API 키 미설정 → `API_KEY_MISSING` 오류
- cert_id 미존재 → `CERT_NOT_FOUND` 오류
- 현재 접수 중인 회차 없음 → 빈 배열 반환 (오류 아님)

### 현재 상태
활성

---

## F-10. 관리자 점검 화면

### 목적
정규화 결과, candidate 생성 결과, validation 결과를 확인할 수 있는 관리 기능을 제공한다.

### 주 사용자
- 운영자

### 입력
- 관리자 요청
- validation result 조회 기준

### 출력
- validation summary
- candidate build 상태
- 오류 목록

### 처리 규칙
- MVP에서는 파일/배치 기반으로 대체 가능하다.
- 화면 기능은 후속으로 둔다.

### 예외 상황
- 관리자 기능 미구현
- validation 데이터 미존재

### 현재 상태
reserved

---

## F-11. 관련 동영상 추천 (YouTube)

### 목적
추천된 자격증의 학습 자료 접근성을 높이기 위해, 자격증명 기반으로 YouTube 강의·인강·합격 영상을 추천한다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자 (`cert_id`)

### 출력
- 영상 리스트 (최대 5개). 각 영상은 아래 정보를 포함한다.
  - `video_id`: YouTube 영상 ID
  - `title`: 영상 제목
  - `channel`: 채널명
  - `thumbnail_url`: 썸네일 URL
  - `url`: 시청 링크 (`https://www.youtube.com/watch?v={video_id}`)
- 캐시 메타: `fetched_at`, `cache_hit`

### 처리 규칙
- 검색 쿼리는 `cert_name`을 사용해 다음 OR 조합으로 구성한다: `"{cert_name} 강의" | "{cert_name} 인강" | "{cert_name} 합격"`.
- YouTube Data API v3의 `search.list` endpoint를 사용하며 `part=snippet`, `type=video`, `maxResults=5`, `regionCode=KR`, `relevanceLanguage=ko`로 호출한다.
- 검색 결과는 Supabase `cert_video_cache` 테이블(§DATA_SCHEMA.md §6.12)에 캐싱한다.
- TTL: **30일**. `fetched_at`이 30일 이내면 캐시를 반환하고, 만료되면 재호출 후 upsert한다.
- 캐시 키는 `cert_id` 단일. 검색 쿼리 구성 정책이 바뀌면 `query_version` 필드를 증가시켜 무효화한다.
- YouTube API quota(검색 100 units/호출, 일일 10,000 units 무료) 초과 시 캐시된 결과가 있으면 반환하고, 없으면 빈 결과 + `quota_exceeded` 에러 메시지를 응답한다.
- 추천 핵심 흐름과 분리된 부가 기능이므로, 본 기능이 실패해도 추천 자체는 정상 동작해야 한다.

### 예외 상황
- `cert_id`가 candidates에 없음 → 404
- YouTube API 키 미설정 → 500 (운영자 알림 필요)
- YouTube API 쿼터 초과 → 캐시 fallback (없으면 빈 배열 + `quota_exceeded`)
- 검색 결과 0건 → 빈 배열 반환 (오류 아님)
- 네트워크 timeout → 캐시 fallback (없으면 503)

### 현재 상태
활성

---

---

## F-12. WorkNet 채용정보 조회 (cert_id 기반 데이터 체인)

### 목적
추천된 자격증과 연결된 WorkNet 실시간 채용정보를 cert_id → NCS → occupation 코드 자동 파생 경로로 조회한다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자 (`cert_id`)
- 선택 입력: 지역명, 표시 개수

### 출력
- 채용공고 목록 (공고명, 회사명, 고용형태, 급여, 마감일, 링크)

### 처리 규칙
- `cert_id` → `cert_lookup_service` → cert_name(keyword) + NCS 대직무코드 → WorkNet occupation 코드
- 직접 문자열 매칭 없이 canonical 데이터 체인만 사용한다.
- `NCS_TO_WORKNET_OCCUPATION` 매핑 테이블로 NCS 코드 → WorkNet 직종 코드 변환
- API: WorkNet `wantedApi.do` (XML 응답)

### 예외 상황
- cert_id 미존재 → `CERT_NOT_FOUND`
- API 키 미설정 → `API_KEY_MISSING`
- 채용정보 0건 → 빈 배열 (오류 아님)

### 현재 상태
활성

---

## F-13. Work24 훈련과정 조회 (cert_id 기반 데이터 체인)

### 목적
추천된 자격증과 연결된 국민내일배움카드 훈련과정을 cert_id → NCS 대직무코드 → Work24 srchNcs1 파라미터 자동 파생 경로로 조회한다.

### 주 사용자
- 사용자

### 입력
- 자격증 식별자 (`cert_id`)
- 선택 입력: 지역명, 페이지 크기

### 출력
- 훈련과정 목록 (과정명, 기관명, NCS 분류, 훈련 기간, 비용, 정부 지원금, 취업률)

### 처리 규칙
- `cert_id` → `cert_lookup_service` → NCS 대직무분류명 → Work24 `srchNcs1` 2자리 코드 파생
- 취업률 기준 내림차순 정렬 (`sortCol=3`)
- 조회 기간: 오늘 ~ 3개월 후
- 직접 문자열 매칭 없이 canonical 데이터 체인만 사용

### 예외 상황
- cert_id 미존재 → `CERT_NOT_FOUND`
- API 키 미설정 → `API_KEY_MISSING`
- 훈련과정 0건 → 빈 배열 (오류 아님)

### 현재 상태
활성

---

## F-14. 과정평가형 자격 조회

### 목적
시험 부담이 큰 4단계 고위험군 청년에게 교육 이수형 자격 취득 경로를 안내한다.

### 주 사용자
- 사용자 (특히 위험군 4단계)

### 입력
- 선택 입력: 종목명 검색어

### 출력
- 과정평가형 자격 목록

### 처리 규칙
- Q-Net `ProcessEvalService/getProcessEvalList` API 사용
- 시험 없이 교육 이수로 취득 가능한 자격을 반환한다.

### 예외 상황
- API 키 미설정 → `API_KEY_MISSING`
- 결과 0건 → 빈 배열 (오류 아님)

### 현재 상태
활성

---

## F-15. 서울시 공공 자원 조회

### 목적
서울시 거주 청년에게 일자리카페, 건강증진센터, 공공예약 서비스 접근 정보를 제공한다.

### 주 사용자
- 사용자

### 입력
- 선택 입력: 구 이름 (예: 강남구)

### 출력
- 일자리카페 위치 및 연락처 목록
- 건강증진센터 위치 및 서비스 목록
- 공공예약 가능 서비스 목록

### 처리 규칙
- 서울 열린데이터광장 API 사용 (JSON 응답)
- 구 이름 필터링은 응답 데이터 내 자치구 필드 포함 여부로 수행

### 예외 상황
- API 키 미설정 → `API_KEY_MISSING`
- 결과 0건 → 빈 배열 (오류 아님)

### 현재 상태
활성

---

## F-16. 오늘의 한 가지 행동 제안

### 목적
위험군 단계와 추천 자격증 정보를 바탕으로 사용자가 오늘 즉시 실행할 수 있는 구체적인 행동 1가지를 제안한다.

### 주 사용자
- 사용자

### 입력
- 위험군 단계 ID (`risk_stage_id`, 예: `risk_0004`)
- 선택 입력: 자격증 ID 목록 (`cert_ids`), 지역명

### 출력
- 행동 유형 (study / training / space / wellness / apply 등)
- 행동 설명
- 관련 자격증명
- 동기 메시지

### 처리 규칙
- 위험군 단계별 행동 템플릿(1~4단계 활성, `risk_0005`는 비활성 보존 × 여러 유형)에서 1개를 선택한다.
- 템플릿에 `{cert_name}`, `{region}` 플레이스홀더가 있으면 실제 값으로 치환한다.
- 위험군이 높을수록(4단계) 낮은 부담 행동(wellness, space 방문)을 우선 제안한다.

### 예외 상황
- 알 수 없는 risk_stage_id → `UNKNOWN_RISK_STAGE` 오류
- 해당 단계 템플릿 없음 → 기본 행동 반환

### 현재 상태
활성

---

## F-17. 취업지원 자원 번들 조회

### 목적
위험군 단계, 관심 도메인, 관심 직무를 기반으로 취업지원 자원(채용정보·훈련과정·일자리카페·과정평가형)을 번들로 조회하여 Step 3 로드맵 화면에 자격증 추천과 함께 제공한다. 자격증과 시각적으로 구분된 색 테마로 표시한다.

### 주 사용자
- 사용자

### 입력
- 위험군 단계 ID (`risk_stage_id`, 예: `risk_0004`) — 필수
- 관심 도메인 ID (`domain_id`) — 선택
- 관심 직무 ID 목록 (`job_ids`) — 선택
- 지역명 (`region`, 예: `서울`) — 선택 (없으면 전국)
- 연결 자격증 ID 목록 (`cert_ids`) — 선택 (훈련과정 NCS 파생에 활용)

### 출력
- `resource_types`: 이번 요청에서 활성화된 자원 유형 목록
- `support_level`: 지원 수준 레이블 (`partial` / `standard` / `full`)
- `bundles`: 자원 유형별 번들 배열
  - `resource_type`: `hiring` | `training` | `job_cafe` | `process_eval`
  - `label`: 화면 표시 레이블 (예: "채용정보", "훈련과정", "일자리카페", "과정평가형 자격")
  - `items`: 실제 조회 결과 (각 서비스 응답 구조와 동일)
  - `count`: 항목 수

### 처리 규칙 — 위험군별 자원 유형 게이팅

| 위험군 단계 | 지원 수준 | 활성 자원 유형 |
|---|---|---|
| 1단계 (취업 안정권) | `partial` — 취업 정보 일부 | `hiring` |
| 2~3단계 (중간 위험군) | `standard` — 역량 강화 지원 | `hiring`, `training` |
| 4단계 (고위험군) | `full` — 다지원 | `hiring`, `training`, `job_cafe`, `process_eval` |

- `hiring` 조회: 관심 직무 이름을 keyword로 WorkNet API 호출. 직무 미지정 시 도메인명을 keyword로 사용.
- `training` 조회: 도메인 → NCS 1차 분류 매핑 테이블로 `ncs_category` 파생 후 Work24 API 호출.
- `job_cafe` 조회: 서울시 열린데이터광장 API. 지역 미지정 시 전체 조회.
- `process_eval` 조회: Q-Net 과정평가형 자격 목록. 도메인/직무 필터 없음.
- 각 자원 조회가 실패(API 키 미설정·타임아웃)해도 다른 자원 조회는 계속 진행하며, 실패한 자원은 `items: []` + `error` 필드로 반환한다.
- 이 기능의 실패가 로드맵 자격증 추천 자체의 실패를 유발하지 않는다.

### 도메인 → NCS 1차 분류 매핑 (대표 예시)

| 도메인 taxonomy | NCS 1차 분류 |
|---|---|
| 데이터/AI | 정보통신 |
| 소프트웨어개발 | 정보통신 |
| 금융/핀테크 | 금융/보험 |
| 건설/토목 | 건설 |
| 기계/제조 | 기계 |
| 화학/바이오 | 화학/바이오 |
| 전기/전자 | 전기/전자 |
| 경영/사무 | 경영/회계/사무 |
| 보건/의료 | 보건/의료 |

매핑 불일치 시 `ncs_category = null`로 Work24 전체 조회.

### 예외 상황
- `risk_stage_id` 미제공 → `MISSING_REQUIRED_FIELD`
- 개별 외부 API 실패 → 해당 번들 `items: []`, `error` 메시지 포함, 나머지 정상 반환
- 모든 번들 `items: []` → 오류 아님, 빈 번들 응답 정상 반환

### 현재 상태
활성

---

## 6. 기능 간 의존성

| 선행 기능 | 후행 기능 | 설명 |
|---|---|---|
| F-01 | F-02 | 위험군 기준이 추천 요청 맥락에 포함된다. |
| F-02 | F-03 | 정규화된 직무/도메인 입력이 후보 검색으로 이어진다. |
| F-03 | F-04 | 추천 결과가 있어야 설명 근거를 조회할 수 있다. |
| F-03 | F-05 | 추천 결과 또는 추천 맥락이 있어야 로드맵 생성이 가능하다. |
| F-06 | F-07 | canonicalization 결과가 있어야 candidate 생성이 가능하다. |
| F-07 | F-03 | candidate row가 recommendation core의 주요 입력이 된다. |
| F-08 / F-09 | F-03 | 일정·링크 기능은 추천 결과의 cert_id와 결합된다. |
| F-12 / F-13 | F-03 | cert_id 기반 채용·훈련 조회는 추천 결과의 cert_id가 입력이다. |
| F-12 / F-13 / F-08 / F-09 | cert_lookup_service | 모든 cert_id 기반 API 호출은 cert_lookup_service 데이터 체인을 경유한다. |
| F-03 | F-11 | 추천 카드에서 자격증을 선택해야 관련 동영상 조회가 가능하다. |
| F-03 | F-16 | 오늘의 행동 제안은 추천 결과의 cert_ids를 선택적으로 받는다. |
| F-01 / F-02 | F-17 | 위험군 단계와 도메인·직무 선택 결과가 취업지원 번들 입력이 된다. |
| F-12 / F-13 / F-15 | F-17 | 취업지원 번들은 기존 hiring·training·seoul 서비스를 위험군에 따라 선택적으로 호출한다. |

---

## 7. 공통 예외처리 원칙

모든 사용자 기능은 아래 공통 원칙을 따른다.

1. 추천 결과가 0건이어도 시스템 오류와 구분해야 한다.
2. 문서 근거가 없더라도 추천 결과는 반환 가능해야 한다.
3. taxonomy 밖 값이 생성되면 validation error로 간주한다.
4. reserved 기능은 명시적으로 비활성 상태로 처리한다.
5. 현재 스프린트 범위 밖 기능을 임의로 fallback 구현하지 않는다.

---

## 8. 현재 활성 기능

현재 시스템에서 실제로 활성화된 기능은 아래와 같다.

**추천 핵심 흐름**
- F-01 위험군 기반 추천 진입
- F-02 관심 직무/도메인 기반 추천 요청
- F-03 자격증 후보 결과 조회
- F-04 추천 이유 / 설명 근거 조회
- F-05 위험군 맞춤 로드맵 조회
- F-11 관련 동영상 추천 (YouTube)

**데이터 정비**
- F-06 CSV canonicalization 실행
- F-07 entity / relation / candidate 재생성

**실행 지원 (Execution Layer)**
- F-08 시험 일정 조회 (Q-Net API)
- F-09 접수 일정 / 지원 링크 조회 (Q-Net API)
- F-12 WorkNet 채용정보 조회 (cert_id 기반)
- F-13 Work24 훈련과정 조회 (cert_id 기반)
- F-14 과정평가형 자격 조회
- F-15 서울시 공공 자원 조회
- F-16 오늘의 한 가지 행동 제안

---

## 9. 후속 문서 연결

이 문서의 각 기능은 아래 문서와 연결된다.

- API 요청/응답 형식 → `API_SPEC.md`
- 데이터 구조 → `DATA_SCHEMA.md`
- retrieval 및 indexing 세부 → `RAG_PIPELINE.md`
- 시스템 계층 책임 → `SYSTEM_ARCHITECTURE.md`

---

## 10. 최종 요약

이 문서는 시스템이 제공하는 기능을 **사용자 기능 단위**로 정의한다.  
핵심 추천 흐름(F-01~F-07, F-11)과 Execution Layer(F-08~F-09, F-12~F-16)가 모두 활성화되었다.

현재 시스템의 기능 중심은 아래 세 축이다.

1. **추천 가능한 구조적 데이터 생성** (F-06~F-07)
2. **추천 결과를 사용자 흐름으로 제공** (F-01~F-05, F-11)
3. **추천 결과에서 실행 행동으로 이어지는 Execution Layer** (F-08~F-09, F-12~F-16)

---

## 11. 데모 제출용 기능 단계 (임시)

> 본 절은 `PRD.md` **§19** 와 대응한다. **제출·시연 기한 동안만** 구현 우선순위로 사용한다.  
> 기능 ID(F-xx) 정의는 상기 본문이 우선이며, 본 절은 **최소 동작·허용 스텁**만 추가한다.

### 11.1 단계·기능 매핑

| 단계 | 목표 | 관련 기능 | 데모 최소 완료 기준 |
|:---:|:---|:---|:---|
| D1 | 시나리오·데이터 고정 | F-01, F-02 | 시연에 쓸 입력값·taxonomy 조합·문서 목록이 고정됨 |
| D2 | 인덱싱 재현 | F-04(검색 입력 전제) | Parse→Chunk→Embed→Store가 문서화된 명령으로 재실행 가능 |
| D3 | 근거 검색 경로 | F-04 | `cert_id`(또는 동등 키)로 snippet 1건 이상 조회 가능; 0건은 예외로 처리 |
| D4 | 추천·로드맵 시연 | F-03, F-05 | 후보 ≥1, 로드맵 단계 ≥1 표시 |
| D5 | LLM 조립(임시) | F-04 | 검색·canonical 필드만으로 응답 생성; 근거 없을 때 사실 생성 금지 (`PROMPT_DESIGN.md`) |
| D6 | 제출·재현 패키지 | F-06~F-07(선택) | subset canonical만으로도 D4가 성립하면 전체 배치는 생략 가능 |

### 11.2 데모 기간 허용 스텁

- **F-06~F-07**: 전체 CSV가 아닌 **데모용 subset 파일**만 canonical/candidate에 반영해도 된다. 단, taxonomy 검증 규칙은 완화하지 않는다.  
- **F-10**: REST/화면 대신 **파일 산출물·CLI exit code**로 validation을 보여도 된다.  
- **F-08~F-09**: Q-Net API 연동 완료 (2026-05-14). 데모에서 실제 일정 데이터 표시 가능.

### 11.3 데모에서 금지하는 것

- reserved 기능을 **완료된 것처럼** 노출하거나 실데이터 일정·링크를 지어내기.  
- retrieval 0건인데 LLM이 **문서에 없는 규정·날짜·URL**을 채우기.  
- taxonomy 밖 `related_jobs` / `related_domains`를 사용자 응답에 포함하기.

### 11.4 제출 후

- 본 절(§11)을 삭제하거나, MVP 본문(§5~§8)과 합치면서 중복 표를 정리한다.

---

## 12. 고립군 청년 지원 파이프라인 (F-18~F-24)

### F-18. 고립군 청년 군집 분류

#### 목적
12문항 설문 응답 결과를 바탕으로 사용자를 4개 고립군 군집 중 하나로 분류한다.  
군집은 단순 총점 구간이 아니라 **행동 유형 패턴(다차원 판별 규칙)**으로 결정한다.

#### 이론적 근거

군집 구분의 핵심은 **관계 어려움 수준** × **활동 자발성** × **은둔 반복성** 3축이다.  
(근거: 「고립은둔 청년 지원사업 모형 개발 연구」, 한국보건사회연구원 2022 / 「서울시 고립·은둔청년 실태조사」 / 생명의전화 종합사회복지관 청년이음센터 실무 가이드북 2021)

#### 입력
- 12문항 설문 응답 (관계망 4, 활동 2, 노동경제 2, 정신건강 3, 자기관리 1)

#### 차원 점수 계산 (각 문항 0~3점)

| 차원 | 문항 구성 | 만점 | 해석 (높을수록) |
|---|---|:---:|---|
| D_관계망 | 관계망 4문항 합산 | 12 | 관계 어려움 높음 |
| D_활동 | 활동 2문항 합산 | 6 | 외출·사회활동 제한 |
| D_노동경제 | 노동경제 2문항 합산 | 6 | 취창업 의지·역량 부족 |
| D_정신건강 | 정신건강 3문항 합산 | 9 | 우울·불안 심각 (Safety Key) |
| D_자기관리 | 자기관리 1문항 | 3 | 일상 관리 어려움 |

#### 군집 판별 규칙 (다차원 의사결정 트리)

```
Step 1. Safety Override
  IF D_정신건강.B12_9 ≥ 2  → 위기 안내 배너 활성 + cluster 강제조정 제외 (아래 계속 진행)

Step 2. 은둔청년 판별 (우선 체크)
  IF D_관계망 ≥ 8 AND D_활동 ≥ 5  → cluster 4 (은둔청년)

Step 3. 활동제한형고립청년 판별
  IF D_관계망 ≥ 6  → cluster 3 (활동제한형고립청년)

Step 4. 활동형고립청년 vs 고립위험청년 구분
  IF D_노동경제 ≥ 3  → cluster 2 (활동형고립청년)
  ELSE               → cluster 1 (고립위험청년)
```

> 임계값(8, 5, 6, 3)은 초기 규칙 기반 값이며, 데이터 수집 후 통계 검증으로 조정한다 (§F-18 유효성 검증 참조).

#### 출력
| 군집 ID | 군집명 | 전략 | 핵심 특성 |
|---|---|---|---|
| 1 | 고립위험청년 | 예방을 위한 연계·추적 관리 | 관계 어려움 낮음, 취창업 노력 중 |
| 2 | 활동형고립청년 | 맞춤형 사례관리 | 관계 어려움 낮음, 사회 안착 반복 실패 |
| 3 | 활동제한형고립청년 | 맞춤형 사례관리 | 관계 어려움 있음, 비자발적 활동 제한 |
| 4 | 은둔청년 | 맞춤형 사례관리 | 외부 접촉 최소화, 은둔 반복 |

#### Safety Override
- `B12_9` (자살·자해 사고) score ≥ 2 → 1393 자살예방상담전화 배너 활성
- cluster 재조정 없이 원래 군집 유지 (서비스 추천은 F-19가 담당)

#### 군집별 서비스 매트릭스
군집별 서비스 카테고리 활성/비활성은 F-19 §서비스 매트릭스 참조.

#### F-18 유효성 검증 계획

> 분류 근거가 믿을 만한지 확인하기 위한 검증 계획 (데이터 수집 후 수행)

| 검증 단계 | 방법 | 목표 기준 |
|---|---|---|
| **내적 일관성** | 차원별 Cronbach's α 계산 | α ≥ 0.70 per dimension |
| **구성 타당도** | 탐색적 요인 분석(EFA) — 5요인 구조 확인 | Factor loading ≥ 0.40 |
| **군집 타당도** | k-means(k=4) 실루엣 점수, 군집 간 F1 | Silhouette ≥ 0.40 |
| **준거 타당도** | 사회복지사 판정과 알고리즘 판정 Cohen's κ | κ ≥ 0.60 |
| **임계값 조정** | ROC 기반 최적 cut-off 탐색 per 차원 | 민감도/특이도 균형 |

- 검증 결과는 `docs/evaluation/` 폴더에 리포트로 보관
- 임계값 변경 시 본 절과 `DATA_SCHEMA.md §4.8` 동시 갱신

#### 현재 상태
활성 (frontend RiskAssessment 구현 완료) — 분류 임계값은 규칙 기반 초기값, 유효성 검증 미완료

---

### F-19. 고립군 군집별 맞춤 정책 추천 번들

#### 목적
서비스 매트릭스에 따라 군집별로 해당 서비스 카테고리에 속하는 정책·기관만 추천한다.

#### 입력
- `cluster_id`: '1'~'4'
- `region`: 거주 지역 (선택, 지자체 서비스 필터링)
- `items_per_category`: 카테고리당 반환 수 (기본 3)

#### 서비스 매트릭스
| 서비스 카테고리 | 고립위험(1) | 활동형(2) | 활동제한형(3) | 은둔(4) |
|---|:---:|:---:|:---:|:---:|
| 자기이해 및 심리상담 | ✅ | ✅ | ✅ | ✅ |
| 치유적 관계형성 | ❌ | ✅ | ✅ | ✅ |
| 일 경험 및 사회활동 | ✅ | ✅ | ❌ | ❌ |
| 사후관리 | ✅ | ✅ | ✅ | ✅ |
| 경제·생계·주거지원 | ✅ | ✅ | ✅ | ✅ |
| 가족지원 | ✅ | ✅ | ✅ | ✅ |
| 통합된 삶 (취업) | ✅ | ❌ | ❌ | ❌ |

#### API 매핑
| 서비스 카테고리 | 1순위 API | 2순위 API |
|---|---|---|
| 자기이해 및 심리상담 | 복지로_중앙부처복지서비스 | 여성가족청소년시설 |
| 치유적 관계형성 | 복지로_지자체복지서비스 | 여성가족청소년시설 |
| 일 경험 및 사회활동 | 워크넷_정부지원일자리_참여자모집 | — |
| 사후관리 | 복지로_지자체복지서비스 | — |
| 경제·생계·주거지원 | 복지로_중앙부처복지서비스 | 복지로_지자체복지서비스 |
| 가족지원 | 건강가정지원센터 | 여성가족청소년시설 |
| 통합된 삶 | 워크넷_채용정보 | — |

#### endpoint
`GET /api/v1/isolation/policy?cluster_id={1-4}&region={지역}&items_per_category={n}`

#### 현재 상태
활성

---

### F-20. 복지로 중앙부처복지서비스 조회

#### 목적
한국사회보장정보원 복지로 OPEN API를 통해 중앙부처 복지서비스 목록을 조회한다.

#### 활용 카테고리
자기이해 및 심리상담 / 경제·생계·주거지원 / 가족지원

#### API
`GET https://apis.data.go.kr/B551537/wlfareSvc01/getWlfareSvcList01`
인증키: `BOKJIRO_API_KEY`

#### 현재 상태
활성

---

### F-21. 복지로 지자체복지서비스 조회

#### 목적
한국사회보장정보원 복지로 OPEN API를 통해 지자체 복지서비스 목록을 조회한다.

#### 활용 카테고리
치유적 관계형성 / 사후관리 / 경제·생계·주거지원

#### API
`GET https://apis.data.go.kr/B551537/wlfareSvc02/getWlfareSvcList02`
인증키: `BOKJIRO_API_KEY`

#### 현재 상태
활성

---

### F-22. 건강가정지원센터 시설 조회

#### 목적
성평등가족부 OPEN API를 통해 건강가정지원센터 시설 목록을 조회한다.

#### 활용 카테고리
가족지원

#### API
`GET https://apis.data.go.kr/B383000/gmis/hFthHomeSprnServiceV2`
인증키: `GENDER_WELFARE_API_KEY`

#### 현재 상태
활성

---

### F-23. 여성·가족·청소년·권익시설 조회

#### 목적
성평등가족부 OPEN API를 통해 여성·가족·청소년·권익 시설 정보를 조회한다.

#### 활용 카테고리
자기이해 및 심리상담 / 치유적 관계형성 / 가족지원

#### API
`GET https://apis.data.go.kr/B553530/GFacility01/getGFacility01`
인증키: `GENDER_WELFARE_API_KEY`

#### 현재 상태
활성

---

### F-24. 워크넷 정부지원일자리 참여자모집 조회

#### 목적
한국고용정보원 워크넷 OPEN API를 통해 정부지원일자리 참여자모집 정보를 조회한다.

#### 활용 카테고리
일 경험 및 사회활동 (고립위험청년·활동형고립청년 전용)

#### API
`GET https://www.work24.go.kr/cm/openApi/call/wk/callOpenApiSvcInfo380L02.do`
인증키: `WORKNET_GOVT_JOBS_API_KEY` (또는 `GET_JOB_API_KEY`)

#### 현재 상태
활성

---

### F-25. DIDIM 서비스 허브 — 군집 기반 전체 서비스 활성/비활성 + LLM 설명

#### 목적
고립군 진단(F-18)의 cluster_id를 받아, didim.life가 보유한 모든 서비스를  
지금 이 군집에게 적합한지 여부로 분류하고, LLM이 이유를 설명한다.

#### 핵심 설계 원칙
- **활성/비활성 결정은 `cluster_service_matrix.py`의 하드코딩 매트릭스**가 담당
- **LLM은 결정에 대한 이유 설명만** 담당 (어떤 서비스를 보여줄지 결정하지 않음)
- LLM 실패 시 fallback은 reason_inactive 텍스트를 그대로 반환

#### 대상 DIDIM 서비스 (13개)

| 서비스 ID | 이름 | 카테고리 | 1 | 2 | 3 | 4 |
|---|---|---|:---:|:---:|:---:|:---:|
| certification_roadmap | 자격증 추천·로드맵 | 취업준비 | ✅ | ✅ | ✅ | ❌ |
| exam_schedule | 시험 일정 조회 | 취업준비 | ✅ | ✅ | ✅ | ❌ |
| youtube_lectures | 자격증 관련 강의 | 취업준비 | ✅ | ✅ | ✅ | ❌ |
| training_courses | 훈련과정 | 취업준비 | ✅ | ✅ | ✅ | ❌ |
| job_listings | 채용정보 | 취업 | ✅ | ❌ | ❌ | ❌ |
| govt_job_programs | 정부지원 일자리 | 사회참여 | ✅ | ✅ | ❌ | ❌ |
| job_cafes | 일자리카페 | 공간지원 | ✅ | ✅ | ❌ | ❌ |
| health_centers | 건강증진센터 | 공간지원 | ✅ | ✅ | ✅ | ✅ |
| welfare_central | 중앙부처 복지서비스 | 복지지원 | ✅ | ✅ | ✅ | ✅ |
| welfare_local | 지자체 복지서비스 | 복지지원 | ✅ | ✅ | ✅ | ✅ |
| family_centers | 건강가정지원센터 | 가족지원 | ✅ | ✅ | ✅ | ✅ |
| gender_facilities | 여성·가족·청소년 시설 | 상담지원 | ✅ | ✅ | ✅ | ✅ |
| today_action | 오늘의 행동 제안 | 실천 | ✅ | ✅ | ✅ | ✅ |

#### 엔드포인트
`GET /api/v1/isolation/dashboard`

| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| cluster_id | str | ✅ | '1'~'4' |
| region | str | ❌ | 거주 지역 |
| survey_summary | str | ❌ | 설문 요약 텍스트 (LLM 개인화용) |
| with_llm | bool | ❌ | LLM 설명 생성 여부 (기본 true) |

#### 출력
```json
{
  "success": true,
  "data": {
    "cluster_id": "2",
    "cluster_meta": {"label": "활동형고립청년", ...},
    "active_services": [
      {"id": "certification_roadmap", "name": "자격증 추천·로드맵",
       "category": "취업준비", "frontend_path": "/roadmap",
       "explanation": "관계망을 쌓아가면서 취업 목표도 함께 세울 수 있습니다."}
    ],
    "inactive_services": [
      {"id": "job_listings", "name": "채용정보", "category": "취업",
       "reason": "지금은 직접 취업보다 사회적 관계 회복과 자신감 형성이 먼저입니다."}
    ],
    "today_action": {"action": {...}, "motivation": "..."},
    "active_count": 12,
    "inactive_count": 1
  }
}
```

#### 현재 상태
활성

---

### F-26. 온통청년 청년정책 조회

#### 목적
온통청년 포털(youthcenter.go.kr) API를 통해 청년정책을 대분류·중분류·지역으로 필터링하여 조회한다.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| lclsf_nm | str | ❌ | 정책대분류: 일자리|주거|교육|복지문화|참여권리 |
| mclsf_nm | str | ❌ | 정책중분류: 취업|재직자|창업|미래역량강화 등 |
| region | str | ❌ | 지역명 (예: 서울, 경기) |
| keyword | str | ❌ | 검색 키워드 |
| page_size | int | ❌ | 결과 수 (기본 5) |

#### 출력
- 정책 목록 (정책ID, 정책명, 대분류, 중분류, 지역, 신청URL, 기간)
- source: `온통청년_청년정책`

#### 처리 규칙
- 대분류(lclsfNm) 필터는 허용값(일자리·주거·교육·복지문화·참여권리)만 통과시킨다.
- 응답 0건이어도 추천 실패로 간주하지 않는다.
- 10분 TTL 캐시 적용.

#### 현재 상태
활성

---

### F-27. 온통청년 청년공간(센터) 조회

#### 목적
온통청년 포털(youthcenter.go.kr) API를 통해 청년센터·청년공간 시설 정보를 지역별로 조회한다.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| region | str | ❌ | 지역명 (예: 서울, 경기) |
| page_size | int | ❌ | 결과 수 (기본 5) |

#### 출력
- 공간 목록 (시설명, 주소, 전화번호, 운영시간, 홈페이지)
- source: `온통청년_청년공간`

#### 처리 규칙
- 10분 TTL 캐시 적용.

#### 현재 상태
활성

---

### F-28. 온통청년 청년콘텐츠 조회

#### 목적
온통청년 포털(youthcenter.go.kr) API를 통해 청년 대상 콘텐츠를 정책대분류(일자리·주거·교육·복지문화·참여권리)에 따라 분류하여 제공한다.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| category | str | ❌ | 정책대분류: 일자리|주거|교육|복지문화|참여권리|전체 (기본 전체) |
| keyword | str | ❌ | 콘텐츠 검색 키워드 |
| page_size | int | ❌ | 결과 수 (기본 5) |

#### 출력
- 콘텐츠 목록 (콘텐츠ID, 제목, 대분류, 콘텐츠URL, 썸네일URL, 등록일)
- categories: 응답에 포함된 대분류 종류 목록
- source: `온통청년_청년콘텐츠`

#### 처리 규칙
- 대분류는 허용값(일자리·주거·교육·복지문화·참여권리)만 반환한다. taxonomy 밖 값은 기타로 분류.
- 카테고리별 결과를 grouped_by_category 필드로도 제공한다.
- 10분 TTL 캐시 적용.

#### 현재 상태
활성

---

### F-29. 일학습병행 훈련과정 조회

#### 목적
Work24 callOpenApiSvcInfo313L01 API를 통해 일학습병행(공동훈련센터형) 훈련과정 목록을 제공한다.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| region | str | ❌ | 지역명 (예: 서울, 경기) |
| ncs_category | str | ❌ | NCS 1차 분류명 (예: 정보통신) |
| course_name | str | ❌ | 훈련과정명 검색어 |
| page_size | int | ❌ | 결과 수 (기본 20, 최대 100) |

#### 출력
- 훈련과정 목록 (course_id, course_name, institution_name, ncs_code, train_start, train_end, train_type=공동훈련센터형, course_url, tel)
- total: 전체 조회 건수
- source: `Work24_일학습병행훈련과정`

#### 처리 규칙
- GET_WORKSTUDY_API_KEY 사용 (Work24 313L01 전용)
- 오늘~6개월 이내 과정 기준 조회
- 5분 TTL 캐시 적용
- 인증키: GET_WORKSTUDY_API_KEY

#### 현재 상태
활성

---

### F-30. 국가인적자원개발 컨소시엄 훈련과정 조회

#### 목적
Work24 callOpenApiSvcInfo312L01 API를 통해 국가인적자원개발 컨소시엄 훈련과정 목록을 제공한다.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| region | str | ❌ | 지역명 (예: 서울, 경기) |
| ncs_category | str | ❌ | NCS 1차 분류명 (예: 정보통신) |
| course_name | str | ❌ | 훈련과정명 검색어 |
| page_size | int | ❌ | 결과 수 (기본 20, 최대 100) |

#### 출력
- 훈련과정 목록 (course_id, course_name, institution_name, ncs_code, train_start, train_end, train_type=국가인적자원개발컨소시엄, course_url, tel, capacity)
- total: 전체 조회 건수
- source: `Work24_국가인적자원개발컨소시엄훈련과정`

#### 처리 규칙
- GET_HUMANDE_API_KEY 사용 (Work24 312L01 전용)
- 오늘~6개월 이내 과정 기준 조회
- 5분 TTL 캐시 적용

#### 현재 상태
활성

---

### F-31. 구직자취업역량 강화프로그램 조회

#### 목적
구직자취업역량 강화프로그램 목록을 제공한다. API 키 발급 완료, Work24 서비스 엔드포인트 승인 대기 중.

#### 주 사용자
사용자

#### 입력
| 파라미터 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| region | str | ❌ | 지역명 |
| keyword | str | ❌ | 검색어 |
| page_size | int | ❌ | 결과 수 (기본 10) |

#### 출력
- 엔드포인트 승인 완료 후: 프로그램 목록
- 현재: `SERVICE_PENDING` 응답 (키 설정 확인 가능)

#### 처리 규칙
- GET_PROGRAM_API_KEY 사용
- Work24 서비스 엔드포인트 승인 후 자동 활성화 예정

#### 현재 상태
대기 (키 발급 완료, 엔드포인트 승인 대기)

---

### F-32. 자격증·진로 Q&A 챗봇 (RAG 근거 기반, 자체 검증)

#### 목적
자격증·직무·로드맵·정부 지원 정책에 대한 사용자 질문에, cert/위험군 evidence 검색 결과와 canonical 데이터를 근거로 답변한다. 모든 페이지에 플로팅 위젯(`ChatWidget`)으로 노출되며, 현재 선택된 위험군 단계·도메인·직무·자격증 컨텍스트를 함께 전달해 질문에 맞춘 evidence를 조회한다.

#### 범위 경계
- 이 기능은 자격증/진로/정부 지원 정책 질의에 한정된 **단일 세션 Q&A**다. 대화 기록은 브라우저 세션에만 존재하고 서버에 저장되지 않는다.
- 치료적 개입, 다회차 상담 이력 관리, 위기 개입 프로토콜은 포함하지 않는다 (위기 신호는 F-01 Safety Override 배너가 별도로 처리).
- `README.md`/`SYSTEM_ARCHITECTURE.md`/`PRD.md`가 reserved(§10 비범위)로 명시한 "완전한 상담형 대화 에이전트"(장기 세션·치료적 개입 포함)와는 범위가 다르다. 그 항목은 여전히 미구현이며, F-32 활성화로 자동 승격되지 않는다 — reserved 목록 정리 여부는 별도 결정이 필요한 열린 이슈다.

#### 핵심 설계 원칙 (자체 검증 가드레일)
- `_retrieve_evidence` / `_retrieve_stage_evidence`로 cert_id·stage_id 기반 evidence snippet을 조회해 시스템 프롬프트에 주입한다. evidence가 없으면 프롬프트에 근거 섹션을 포함하지 않는다.
- 답변 생성 후 `_self_evaluate_reply`가 휴리스틱으로 재검증한다: (1) 구체적 날짜(연/월/일) 언급, (2) evidence 없이 "합격률/통과율" 문맥으로 등장한 % 수치, (3) 허용 도메인 목록 밖의 링크. (2)는 "합격/통과" 문맥에 인접한 %만 감지해 시스템 프롬프트에 이미 있는 정책 수치(훈련비 지원 45~85% 등)를 오탐하지 않는다.
- 문제가 감지되면 문제 목록을 모델에 전달해 **1회 재생성**한다(`llm_roadmap_service._self_evaluate`와 동일한 self-refine 패턴). 재생성 실패 시 원본 답변을 그대로 반환한다.
- 응답에 `eval.issues`, `eval.refined`를 포함해 재검증 이력을 노출한다.

#### 엔드포인트
`POST /api/v1/chat`

| 필드 | 타입 | 필수 | 설명 |
|---|---|:---:|---|
| messages | array | ✅ | `{role: 'user'\|'assistant', content: string}[]`, 최근 10개만 사용 |
| context.stage_id | str | ❌ | 위험군 단계 (evidence 검색용) |
| context.cert_id / cert_name | str | ❌ | 현재 보고 있는 자격증 (evidence 검색용) |
| context.domain_name / job_name | str | ❌ | 관심 도메인/직무 (시스템 프롬프트 컨텍스트) |

#### 출력
```json
{
  "success": true,
  "data": {
    "reply": "...",
    "role": "assistant",
    "used_evidence": true,
    "eval": {"issues": [], "refined": false}
  }
}
```

#### 예외 처리
- `openai_api_key` 미설정 → `NOT_CONFIGURED`
- `messages`에 유효한 메시지 없음 → `MISSING_REQUIRED_FIELD`
- OpenAI 호출 실패 → `UPSTREAM_ERROR` (프론트는 "일시적인 오류" 메시지로 표시, 대화 흐름 유지)
- evidence 검색 실패는 조용히 무시하고 evidence 없이 진행 (§7 공통 예외처리 원칙 2항)

#### 현재 상태
활성 (범위 한정 — 위 "범위 경계" 참고)

---

### F-33. 재방문 이어가기 + 오늘의 행동 스트릭 (클라이언트 전용)

#### 목적
완료된 위험군 진단과 "오늘의 행동" 완료 이력을 `localStorage`(60일 TTL)에 남겨, 사용자가 다음 날 이후 재방문했을 때 처음부터 12문항을 다시 풀지 않고 이어갈 수 있게 한다. 서버·계정 없이 클라이언트에서만 동작한다.

#### 배경 / 이전 상태와의 차이
기존 `pipelineState.ts`는 `sessionStorage` 기반이라 탭을 닫으면 위험군 단계·관심 도메인이 전부 사라져, 재방문 시 이어지는 경험이 구조적으로 불가능했다. `userHistory.ts`(`frontend/src/utils/userHistory.ts`)를 신설해 `localStorage` 기반으로 분리했다.

#### 동작
- `RiskAssessment` 결과 화면 진입 시 `recordDiagnosisComplete(stageId, stageLabel)` 호출 — 진단 완료 시점에 자동 기록(버튼 클릭 여부와 무관).
- `IsolationDashboard`의 "오늘의 행동" 카드에 "오늘 했어요" 완료 버튼 추가. 클릭 시 `markTodayActionComplete()` 호출:
  - 어제도 완료했으면 스트릭 +1, 오늘 이미 완료했으면 중복 카운트 없음, 그 외엔 스트릭 1로 리셋.
  - 스트릭 2 이상이면 "🔥 N일째" 배지로 대체 표시.
- `Home` 진입 시 `loadUserHistory()`로 이력 확인 — `lastStage`가 있으면 히어로 위에 "다시 오셨네요" 웰컴백 카드를 표시하고 `/isolation/dashboard?cluster_id=...`로 바로 연결. "새로 진단하기"로 이력 초기화 가능.

#### 처리 규칙
- 서버 저장·계정 시스템 없음 — 전부 브라우저 `localStorage`, 기기/브라우저를 바꾸면 이어지지 않는다(알려진 제약, 후속 범위에서 로그인 기반 영속화 고려 가능).
- 60일간 미사용 시 TTL 만료로 자동 초기화.
- `pipelineState.ts`(세션 내 단계 전달)와 역할을 분리 — `userHistory.ts`는 세션을 넘나드는 재방문 인식 전용.

#### 현재 상태
활성
