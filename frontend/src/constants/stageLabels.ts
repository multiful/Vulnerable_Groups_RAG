// Content Hash: SHA256:69d22b77553909eb624d289d763d91f363aa918a03caf9d63d591f6c84df7d2b
/* ─────────────────────────────────────────────────────────────────────
   위험군 단계 표시용 공용 라벨 (비낙인 / plain-language)

   CLAUDE.md §6 기준 1~4단계 체계는 내부 분류명일 뿐, UI에 그대로 노출하지
   않는다. 특히 "은둔 청년" 같은 임상/정책 문헌 용어는 사용자 화면에 직접
   렌더링하지 않는다 (critique 2026-07-02 P0: Recommendation 결과 화면에서
   "4단계 (은둔 청년)"가 굵게 노출되던 문제).

   아래 문구는 새로 만든 카피가 아니라, 기존에 InterestSelection에서 이미
   쓰이고 있던 RISK_STAGE_LABELS와 동일한 문구를 그대로 복사한 것이다.
   Recommendation은 이 공용 상수를 import해서 쓴다.
   InterestSelection의 자체 RISK_STAGE_LABELS는 이번 변경 범위 밖이라
   그대로 남아 있다 (문구 중복 — 후속 정리 대상, FOLDER.md 참고).
   ───────────────────────────────────────────────────────────────────── */

export const STAGE_LABELS: Record<string, string> = {
  '1': '관계나 활동에서 살짝 어려움이 있는 상황',
  '2': '사회 안착을 반복 시도 중인 상황',
  '3': '사회활동이 다소 제한된 상황',
  '4': '지금 집에서 혼자 있는 시간이 많은 상황',
};
