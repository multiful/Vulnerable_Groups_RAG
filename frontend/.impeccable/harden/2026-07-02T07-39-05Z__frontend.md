# Harden Pass — 2026-07-02T07:39:05Z

## Fixes Applied

### P0 — Auto-advance removed (RiskAssessment `select()`)
- Removed `setTimeout(() => setCurrent(c+1), 260)` from `select()`
- `select()` now only saves the answer; `goNext()` + "다음" button is the only advancement path
- Also simplified: removed `nextCurrent` variable, save progress at `current` not `current+1`
- Keyboard arrow-key handler still calls `select()` (correct — records the highlighted option, user presses 다음 to advance)

### P0 — Safety banner repositioned + restyled (RiskAssessment result page)
- Moved from above the result card (first thing after page-header) to after the CTA buttons
- Changed palette: `var(--danger-light)` / `rgba(244,63,94,.25)` → `var(--primary-light)` / `rgba(37,99,235,.2)`
- Icon: `AlertTriangle` → `Phone` (added to lucide-react import)
- Title copy: "정서적으로 힘드신 것 같아요" → "지금 많이 힘드신 분을 위해"
- Sub copy: clinical framing → supportive framing
- CTA text: "1393 자살예방상담전화에 전화하기" → "1393 자살예방상담전화" (less alarming label, same href)

### P1 — Safety gate palette (RiskAssessment CSS)
- `.safety-gate-card` background: `#f0fdf4` → `var(--warning-light)`
- `.safety-gate-card` border: `#86efac` → `rgba(245,158,11,.3)` (amber, consistent with warning tokens)

### P1 — CertCard WCAG 4.1.2 violation fixed (Recommendation)
- Removed `role="button"`, `tabIndex={0}`, `onKeyDown` from outer card `<div>`
- Converted `<span className="cert-click-hint">` → `<button type="button" className="cert-click-hint" onClick={(e)=>{e.stopPropagation();...}}>` with reset CSS
- Label changed "클릭하여 상세 보기" → "상세 보기" (appropriate for a real button)
- Mouse users retain large click area via `div onClick`; keyboard users Tab to "상세 보기" or "로드맵" — both real buttons

### P1 — `aria-expanded` on accordion headers (InterestSelection)
- Domain group header `<button>`: added `aria-expanded={isOpen}`
- Job group header `<button>`: added `aria-expanded={isOpen}`
- Job section toggle `<button>`: added `aria-expanded={jobSectionOpen}`

## Already-Fixed (confirmed externally before this pass)
- `getEffectiveMax()`: excludes undefined safetyKey questions from scoring denominator — already in file
- B12_9 skip path: `delete newAnswers.B12_9` + `setSafetyFlag(true)` — already in file
- `RISK_LABEL` clinical strings: replaced by `STAGE_LABELS` import from `src/constants/stageLabels.ts` — already in file

## TypeScript
Exit 0 — no errors after all edits.
